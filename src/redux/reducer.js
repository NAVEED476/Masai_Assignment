
const initalstate = {
    users:[],
    user:{},
    loading:false
}

const  userreducer = (state=initalstate,action)=>{
    switch(action.type){
        default:
            return state
    }
}


export default userreducer;